{
    "id": "example-01",
    "description": "Basic happy path",
    "input": "{{ example_input if example_input else 'TODO: add example input' }}",
    "expected_behavior": "{{ example_output if example_output else 'TODO: describe expected behavior' }}",
    "should_not": "{{ bad_output if bad_output else 'TODO: describe bad output' }}"
}
